<div class="container-fluid px-4 M">
    <div class="card mt-4"></div>
    <h3 class="mt-4 font-weight-bold mb-4">관리자</h3>
    <div class="card mb-4">
        <div class="btn-group" role="group" aria-label="Basic example">
            <a type="button" class="btn btn-success tab" href='/admin?page=1&tab=today'>일</a>
            <a type="button" class="btn btn-success tab" href='/admin?page=1&tab=week'>주</a>
            <a type="button" class="btn btn-success tab" href='/admin?page=1&tab=month'>월</a>
            <a type="button" class="btn btn-success tab"  href='/admin?page=1&tab=all'>전체</a>
        </div>
        <div class="card-body">
            <div class="row">
                <table class="table rounded">
                    <thead class="bg-light">
                    <tr>
                        <th>No<button class="btn" id="sortTotal" value="1"><i class="fas fa-sort"></i></button></th>
                        <th>이메일<button class="btn" id="sortTotal" value="1"><i class="fas fa-sort"></i></button></th>
                        <th>총 투입량
                            <button class="btn" id="sortTotal" value="1"><i class="fas fa-sort"></i></button>
                        </th>
                        <th>캔
                            <button class="btn" id="sortCan" value="1"><i class="fas fa-sort"></i></button>
                        </th>
                        <th>플라스틱
                            <button class="btn" id="sortPlastic" value="1"><i class="fas fa-sort"></i></button>
                        </th>
                        <th>유리
                            <button class="btn" id="sortGlass" value="1"><i class="fas fa-sort"></i></button>
                        </th>
                        <th>super<button class="btn" id="sortTotal" value="1"><i class="fas fa-sort"></i></button></th>
                    </tr>
                    </thead>
                    <tbody id="trashDatas" >
                    <% trashData.forEach( function(item, index){ %>
                        <tr onclick="location.href='/detail?id=<%= item._id %>'" class="btn-white text-dark">
                            <th scope="row"><%= index + 1 %></th>
                            <td><%= item.email %></td>
                            <td><%= item.total %></td>
                            <td><%= item.can %></td>
                            <td><%= item.plastic %></td>
                            <td><%= item.glass %></td>
                            <td><%= item.super %></td>
                        </tr>
                    <% }) %>
                    </tbody>
                    <tfoot>
                    <tr>
                        <td colspan="7">
                            <div class="dataTables_paginate paging_simple_numbers">
                                <ul class="pagination justify-content-center">
                                    <% if (page > 1){ %>
                                        <li class="paginate_button page-item previous btn-success">
                                            <a href="/admin?page=<%= page * 1 - 1 %>"
                                               class="page-link bg-light border text-success">Previous</a>
                                        </li>
                                    <% } %>
                                    <% for (var j = start_page; j < start_page + 10; j++){ %>
                                        <% if (j <= max_page){ %>
                                            <li class="paginate_button page-item">
                                                <a href="/admin?page=<%= j %>"
                                                   class="page-link bg-light border text-success"><%= j %></a>
                                            </li>
                                        <% } %>
                                    <% } %>
                                    <% if (page < max_page){ %>
                                        <li class="paginate_button page-item next">
                                            <a href="/admin?page=<%= page * 1 + 1 %>"
                                               class="page-link bg-light border text-success">Next</a>
                                        </li>
                                    <% } %>
                                </ul>
                            </div>
                        </td>
                    </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>